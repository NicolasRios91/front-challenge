import { StyledInput } from "./styles";
import { SEARCH_PLACEHOLDER } from "../../utils/constants";

// todo add icon and reducer
export const Search = () => {
  return <StyledInput placeholder={SEARCH_PLACEHOLDER} />;
};
